# MaterialGraph Audit Resolutions — 2026-08

Append-only remediation and verification evidence. Current status is authoritative only in `../AUDIT_REGISTER.md`.

# MG-AUD-093

Title

Public validation accepts nonexistent chemical symbols

Status

✅ Resolved

Resolution Version

Post-v1.9.18

Affected Components

- Canonical periodic-table domain data
- Shared discovery element-filter dependency
- Seven discovery GET routes
- `avoid_element` and `prefer_element` query parameters
- Domain and API regression tests

Root Cause

Public discovery routes applied only string-length constraints to element
filters. Structurally valid but nonexistent symbols could therefore enter
discovery processing. The database could not serve as the validator because
its element table contains only the subset currently represented by imported
MaterialGraph data, not the full periodic table.

Resolution

✓ Added a centralized catalogue of all 118 recognized chemical element
symbols.

✓ Added canonical symbol normalization, including conventional casing and
surrounding-whitespace normalization.

✓ Added one shared FastAPI dependency that validates and normalizes both
element-filter parameters.

✓ Applied the dependency consistently to candidates, chains, graph, subgraph,
path, connected-community, and modularity-community routes.

✓ Preserved the distinction between chemical legitimacy and dataset coverage:
a valid symbol such as `Cu` is accepted even when no current material record
uses it, while an unknown symbol such as `Xx` returns `422`.

Regression Verification

✓ Domain tests verify that the catalogue contains exactly 118 symbols,
including `H` and `Og`, and that every canonical symbol normalizes to itself.

✓ Domain tests verify mixed-case normalization, surrounding-whitespace
normalization, and rejection of nonexistent symbols.

✓ API regressions cover all seven affected routes and both element-filter
parameters.

✓ The focused tests, full regression suite, and Ruff checks passed.

API Contract

Yes. Nonexistent chemical symbols now receive `422`, and accepted symbols are
normalized before reaching discovery services. Valid symbols are not rejected
merely because MaterialGraph has no current dataset coverage for them.

Database Migration

No.

Scientific Impact

Invalid element-filter requests can no longer influence discovery results.
No scoring, ranking, graph, or pathway algorithm changed. This remediation
validates symbol identity only; it does not claim that MaterialGraph contains
research data for every recognized element.

Lessons Learned

Chemical-domain validity and current database coverage are separate concerns
and require separate representations. A partial research dataset must not be
used as the authority for the existence of chemical elements.

Related Findings

- MG-AUD-090

# MG-AUD-060

Title

Pending graph-job claiming is not concurrency-safe

Status

✅ Resolved

Resolution Version

Post-v1.9.18

Affected Components

- `GraphJobService.claim_next_pending_job()`
- PostgreSQL graph-job claim transactions
- Multi-worker graph-job execution
- Graph-job service integration tests
- Isolated PostgreSQL test-database procedure

Root Cause

The service selected the oldest `PENDING` job and changed it to `RUNNING`
without locking the selected row. Two workers could therefore read the same
pending row before either transaction committed, allowing duplicate execution.

Resolution

✓ Added `FOR UPDATE SKIP LOCKED` to pending-job selection.

✓ Restricted selection to `PENDING` jobs and retained a one-row claim limit.

✓ Added a stable final job-ID tie-breaker after creation time so equal-time
claims remain deterministic.

✓ Changed the selected job to `RUNNING`, recorded its start time, and committed
the claim transaction before returning the job to a worker.

Regression Verification

✓ PostgreSQL integration coverage verifies that a locked oldest job is skipped
and the next eligible pending job is claimed.

✓ PostgreSQL integration coverage verifies that no job is returned when the
only pending job is locked by another transaction.

✓ Service regressions verify that a claimed `RUNNING` job is not claimed again,
that two pending jobs are claimed in deterministic oldest-first order, and
that no-pending-job behavior returns `None`.

✓ The focused graph-job tests and the full regression suite passed against the
isolated PostgreSQL database `materialgraph_test`.

Test-Database Safety Evidence

The temporary Windows Command Prompt `DATABASE_URL` override was verified
before migrations, seeding, and tests:

```cmd
py -c "from app.core.database import engine; print(engine.url.database)"
```

The deterministic reference baseline was restored and verified before the
full suite:

```text
materials: 28
material 5: (5, 'mp-19017', 'LiFePO4')
```

After testing, the temporary override must be removed in the same Command
Prompt so normal `.env` configuration becomes authoritative again:

```cmd
set DATABASE_URL=
```

The effective database must be verified before any subsequent migration,
seeding, development, or deployment operation. Credentials and complete
connection strings are not recorded in audit documents.

API Contract

No public API schema change. Internally, concurrent workers now receive
distinct claimable jobs instead of being able to claim the same pending row.

Database Migration

No schema change was required for concurrency-safe claiming.

During isolated test-database reconstruction, downgrading the graph-job
migration left the PostgreSQL `job_status` enum behind, causing the next
upgrade to fail until the orphaned type was removed. This migration downgrade
hygiene issue is not part of the MG-AUD-060 concurrency defect and remains an
explicit follow-up.

Scientific Impact

No scientific score, ranking, graph, pathway, or evidence semantic changed.
The remediation prevents duplicate execution of the same queued graph job
under concurrent workers.

Scope Boundary

MG-AUD-060 closes atomic pending-job claiming only. `complete_job()` and
`fail_job()` still permit contradictory lifecycle transitions and remain
tracked under MG-AUD-061.

Lessons Learned

Queue correctness must be verified on the production database dialect with
independent concurrent transactions. A passing single-session state-transition
test does not establish atomic multi-worker claiming.

Related Findings

- MG-AUD-061
- MG-AUD-092

# MG-AUD-061

Title

Graph-job lifecycle permits contradictory state transitions

Status

✅ Resolved

Resolution Version

Post-v1.9.18

Affected Components

- `GraphJobService.complete_job()`
- `GraphJobService.fail_job()`
- PostgreSQL terminal-state transition transactions
- Graph-job lifecycle and concurrency tests
- Isolated PostgreSQL test-database procedure

Root Cause

Terminal update methods loaded a job by ID and then overwrote its status
without requiring the persisted current state to be `RUNNING`. Pending jobs,
already completed jobs, and already failed jobs could therefore be moved
directly or contradictorily into another terminal state. A read-then-check
implementation would also leave competing completion and failure transactions
vulnerable to a race.

Resolution

✓ Replaced unconditional terminal mutations with atomic conditional updates
that match both the job ID and `status = RUNNING`.

✓ Restricted successful lifecycle transitions to `RUNNING -> COMPLETED` and
`RUNNING -> FAILED`.

✓ Made invalid, repeated, contradictory, and missing-job transitions return
`None` without changing persisted state.

✓ Used one transition timestamp for both `completed_at` and `updated_at`.

Regression Verification

✓ Service tests verify successful completion and failure only after a job has
been claimed and changed to `RUNNING`.

✓ Regressions verify rejection from `PENDING`, rejection after `COMPLETED` and
`FAILED`, repeated terminal attempts, unknown job IDs, and timestamp equality.

✓ A PostgreSQL concurrency test runs competing completion and failure attempts
and verifies that exactly one atomic terminal transition succeeds.

✓ The full regression suite passed with 545 tests.

✓ Ruff checks passed for `app/services/graph_job_service.py` and
`tests/services/test_graph_job_service.py`.

Test-Database Safety Evidence

The verification ran against the isolated PostgreSQL database
`materialgraph_test`. After testing, the temporary Windows Command Prompt
override was removed, and the effective normal database was explicitly
verified as `materialgraph`. Credentials and complete connection strings are
not recorded in audit documents.

API Contract

No public API schema changed. The internal service contract now returns an
updated `GraphJob` only when an atomic transition from `RUNNING` succeeds and
returns `None` for missing jobs or invalid current states.

Database Migration

No schema migration was required for lifecycle enforcement. The previously
observed downgrade issue that leaves the PostgreSQL `job_status` enum behind is
separate from MG-AUD-061 and remains an explicit migration-hygiene follow-up.

Scientific Impact

No scientific score, ranking, graph, pathway, or evidence semantic changed.
Persisted job status can no longer be overwritten by a contradictory or
out-of-order terminal transition.

Lessons Learned

Lifecycle validation must be part of the database mutation predicate. A
service-level read followed by a status check is insufficient when competing
workers can attempt different terminal outcomes concurrently.

Related Findings

- MG-AUD-060
- MG-AUD-092

# MG-AUD-063

Status

✅ Resolved

Resolution Version

Post-v1.9.18

Affected Components

- `MaterialNeighborhoodService.get_neighborhood()`
- Limited neighborhood node and edge selection
- Neighborhood graph-closure regression tests

Root Cause

The neighborhood service independently truncated its deterministically sorted
node and edge collections. An edge could survive its own limit even when one
of its endpoint nodes had been removed by the node limit, producing a dangling
source or target reference in the response.

Resolution

✓ Kept the existing deterministic node ordering and node limit.

✓ Built the returned node-ID set from the limited nodes.

✓ Filtered edges so both `source_material_id` and `target_material_id` must be
present in the returned node-ID set before applying the edge limit.

✓ Preserved accurate `node_count` and `edge_count` values from the returned
collections.

Regression Verification

✓ Four focused service tests verify that restrictive limits cannot produce
dangling edges, `limit=1` returns only the root and no edges, a sufficiently
large limit retains all eligible edges, and response counts match collection
lengths.

✓ The full regression suite passed with 549 tests.

✓ Ruff passed for `app/services/material/neighborhood_service.py` and
`tests/services/material/test_neighborhood_service.py`.

Test-Database Safety Evidence

Verification ran against the isolated PostgreSQL database
`materialgraph_test`. The normal local development database remains separate
and is not a pytest target.

API Contract

No public schema changed. Limited neighborhood responses now enforce the
existing graph-closure expectation: every returned edge references two nodes
present in the same response.

Scientific Impact

No scientific score, ranking, pathway, or evidence semantic changed. The
returned neighborhood topology is now internally valid after response limits
are applied.

Lessons Learned

Graph node and edge limits cannot be applied independently. Edge selection
must be constrained by the final returned node set.

Related Findings

- MG-AUD-072
- MG-AUD-082

# MG-AUD-071

Status

✅ Resolved

Resolution Version

Post-v1.9.18

Affected Components

- `MaterialSimilarityService.get_similar_materials()`
- Similarity criticality loading
- Similarity-service regression tests

Root Cause

The similarity service loaded criticality separately for the source material
and each limited candidate. Its request-local cache avoided duplicate lookups
but did not prevent query count from growing with the number of distinct
candidates.

Resolution

✓ Selected the limited candidate set before loading criticality.

✓ Bulk-loaded criticality once for the source material and all limited
candidate material IDs through `get_material_criticality_bulk()`.

✓ Reused the bulk result map while calculating candidate criticality deltas,
directions, explanations, and ranking keys.

✓ Preserved `None` for unknown criticality and retained existing deterministic
ranking semantics.

Regression Verification

✓ Sixteen focused similarity-service tests passed, including regression
coverage that verifies exactly one bulk call for the source and limited
candidates, no single-material criticality calls, and preserved unknown-value
semantics.

✓ The full regression suite passed with 551 tests.

✓ Ruff passed for `app/services/material/similarity_service.py` and
`tests/services/material/test_similarity_service.py`.

API Contract

No public schema or response semantic changed. Similarity results retain their
existing scores, criticality fields, explanations, and deterministic ordering.

Database Migration

No.

Scientific Impact

No scientific score, ranking policy, or evidence semantic changed. The
remediation reduces database query amplification only.

Lessons Learned

Request-local caching prevents duplicate work but does not eliminate N+1 query
growth across distinct candidates. Candidate-dependent evidence should be
loaded in bulk after the bounded candidate set is known.

Related Findings

- MG-AUD-047
- MG-AUD-073

# MG-AUD-021-025

Title

Explicit risk and criticality evidence semantics

Status

✅ Resolved

Resolution Version

Post-v1.9.18

Affected Components

- Shared material evidence policy
- Material risk service and public response schema
- Material criticality service and public response schema
- Scalar and bulk risk compatibility APIs
- Risk, criticality, and material-risk API regression tests

Root Cause

Risk and criticality reused dimensions from the latest element risk profile
without explicitly declaring their shared evidence basis or distinct
aggregation policies. A score calculable from only one available dimension
could appear evidence-complete because completeness measured only calculable
element coverage. Legacy numeric risk APIs also converted unknown evidence to
`0.0`.

Resolution

✓ Added a centralized evidence policy declaring the shared profile source,
shared dimensions, metric-specific dimensions, and aggregation methods.

✓ Preserved historical score eligibility for partial profiles while exposing
available and expected dimension counts, dimension coverage, complete-profile
coverage, and partial-profile identities.

✓ Redefined risk and criticality evidence completeness to require every
expected dimension for every material element.

✓ Preserved the intended aggregation distinction: risk equally averages
calculable element scores, while criticality composition-weights them.

✓ Changed unknown public, scalar, and bulk material risk from numeric `0.0` to
`None`/JSON `null`.

Regression Verification

✓ Twenty-five focused risk, criticality, and API tests passed.

✓ The full regression suite passed with 581 tests.

✓ Ruff passed for all changed services, schemas, the shared policy module, and
the affected tests.

API Contract

Yes. Material risk is now nullable when evidence is unknown. Risk and
criticality responses expose evidence provenance, dimension coverage,
complete-profile coverage, completeness, and aggregation policy metadata.

Database Migration

No.

Scientific Impact

No existing calculable score formula changed. The remediation prevents
partial or missing evidence from being presented as complete or as favorable
numeric zero, and makes the non-independent evidence basis of risk and
criticality explicit.

Related Findings

- MG-AUD-002
- MG-AUD-003
- MG-AUD-008
- MG-AUD-029
- MG-AUD-062
- MG-AUD-065

# MG-AUD-041-043

Status

✅ Resolved

Resolution

Canonical two-decimal scientific-score semantics now govern tie grouping,
top-pathway selection, pairwise comparison, and endpoint-sensitive ranking.
Top selection derives from canonical scores rather than supplied rank, tied
pathways retain stable unique identity, and comparison summaries consistently
represent ties and lower-ranked advantages.

Regression Verification

✓ Twenty-nine comparative-intelligence tests and six endpoint-ranking tests
passed; the full suite passed with 569 tests; Ruff passed.

Related Findings

- MG-AUD-085

# MG-AUD-072

Status

✅ Resolved

Resolution

Neighborhood traversal now applies the response limit while selecting and
expanding nodes, bounds descendant work, preserves deterministic ordering, and
retains only closed edges between returned nodes.

Regression Verification

✓ Eight focused tests and 555 full-suite tests passed; Ruff passed.

# MG-AUD-073

Status

✅ Resolved

Resolution

Substitution analysis now bulk-loads candidate element membership and risk
signals once for the bounded candidate set rather than performing
candidate-dependent queries. Nullable risk and deterministic ordering are
preserved.

Regression Verification

✓ Six focused tests and 556 full-suite tests passed; Ruff passed.

# MG-AUD-078

Status

✅ Resolved

Resolution

Discovery graph construction now validates each transition before admitting
its edge or target node. Rejected transitions therefore cannot leave
disconnected candidate nodes in the returned graph or adjacency structure.

Regression Verification

✓ Seven focused graph-builder tests and 557 full-suite tests passed; Ruff
passed.

# MG-AUD-081

Status

✅ Resolved

Resolution

Discovery graph responses distinguish requested depth from the effective
maximum hops enforced by the graph builder, including empty-material results.

Regression Verification

✓ Fifteen focused traversal tests and 560 full-suite tests passed; Ruff passed.

# MG-AUD-082

Status

✅ Resolved

Resolution

Traversal result limiting selects the final node set first and retains only
edges whose endpoints are both returned. `limit=1` returns only the root with
no dangling edges, and response counts describe the returned collections.

Regression Verification

✓ Eighteen focused traversal tests and 563 full-suite tests passed; Ruff
passed.

# MG-AUD-083

Status

✅ Resolved

Resolution

Subgraph family filtering now occurs before the result limit is applied, so
eligible nodes are not lost because unrelated source-graph nodes consumed the
limit. Subgraph metadata describes the filtered, limited response.

Regression Verification

✓ Twenty focused traversal tests and 565 full-suite tests passed; Ruff passed.

# MG-AUD-085

Status

✅ Resolved

Resolution

Comparative and endpoint-sensitive research ranking now use the same canonical
two-decimal score and tie semantics from a shared policy module.

Regression Verification

✓ Twenty-nine comparative-intelligence tests, six endpoint-ranking tests, and
569 full-suite tests passed; Ruff passed.

Related Findings

- MG-AUD-041
- MG-AUD-042
- MG-AUD-043

# MG-AUD-094

Status

✅ Resolved

Resolution

Discovery analytical routes now enforce bounded positive result limits at the
public API boundary. Equivalent graph and subgraph inputs receive consistent
validation, and community routes use validated response models.

Regression Verification

✓ Twenty-three focused API validation tests and 575 full-suite tests passed;
Ruff passed.

# MG-AUD-028-029

Title

Evidence-origin transparency and endpoint risk-coverage propagation

Status

✅ Resolved

Resolution Version

Post-v1.9.18

Affected Components

- Research evidence intelligence service
- Research evidence response schemas
- Material quality evidence metadata
- Endpoint-sensitive research ranking
- Evidence, quality, endpoint-ranking, and pathway-analysis regression tests

Root Cause

Research evidence summaries used generic supporting-signal confidence and
readiness terminology for deterministic internal rules, which could be read as
external scientific validation. Separately, material quality and endpoint
ranking retained a numeric risk value without carrying its complete-profile
and dimension coverage, allowing partial low-risk evidence to receive a
favorable endpoint risk band.

Resolution

✓ Classified research support as internal deterministic signals and scoped
readiness to internal research prioritization only.

✓ Added explicit external-evidence integration status, internal signal origin,
scientific validation status, confidence scope, and a researcher decision
boundary.

✓ Classified missing literature, experimental, DFT, electrochemical, and
manufacturing evidence as external evidence that is not integrated.

✓ Propagated risk score eligibility, complete-profile coverage, dimension
coverage, completeness, partial-profile identities, unknown elements,
provenance, dimensions, and aggregation policy through material quality.

✓ Preserved raw partial risk for transparency but made endpoint risk-band
eligibility require known and evidence-complete risk.

✓ Preserved quality formulas, readiness thresholds, scientific usefulness
scores, and the existing endpoint ordering dimensions.

Regression Verification

✓ Forty-seven focused evidence, quality, endpoint-ranking, and pathway-analysis
tests passed.

✓ The full regression suite passed with 584 tests.

✓ Ruff passed for all seven changed implementation and test files.

API Contract

Yes. Evidence summaries and endpoint-quality evidence now expose additional
origin, scope, validation, coverage, completeness, and provenance metadata.
Existing fields remain available.

Database Migration

No.

Scientific Impact

No scientific score formula or readiness threshold changed. Internally derived
support can no longer be presented without its unvalidated scope, and partial
risk evidence cannot act as favorable endpoint-ordering evidence.

Related Findings

- MG-AUD-008
- MG-AUD-021
- MG-AUD-023
- MG-AUD-024
- MG-AUD-025
- MG-AUD-062
- MG-AUD-065
- MG-AUD-085

# MG-AUD-056-058

Title

Canonical installation, API-key, and runtime-version authorities

Status

✅ Resolved

Resolution Version

0.1.0 / Post-v1.9.18

Affected Components

- Runtime and development dependency installation
- Package and FastAPI version metadata
- Application health response
- Materials Project importer configuration
- Getting-started and deployment guides
- Project-configuration regression tests

Root Cause

The frozen requirements installed an editable, pinned checkout of
MaterialGraph from a different repository location. Getting-started
documentation configured `MP_API_KEY` while Pydantic Settings and the importer
consumed `MATERIALS_PROJECT_API_KEY`. Package metadata declared version `0.1.0`
while Settings and FastAPI independently declared `1.0.0`.

Resolution

✓ Removed the self-referential MaterialGraph VCS dependency from
`requirements.txt`.

✓ Updated setup and deployment instructions to clone the canonical
`MaterialGraph/materialgraph` repository and install the current checkout.

✓ Standardized Materials Project configuration on
`MATERIALS_PROJECT_API_KEY` across documentation, template, Settings, importer,
and error reporting.

✓ Made installed `pyproject.toml` package metadata the sole release-version
authority through `app.version`.

✓ Removed the configurable and hard-coded `1.0.0` authorities, added the
non-release `0+unknown` fallback, and exposed the resolved version through
FastAPI and `/health`.

✓ Added regression tests preventing dependency, environment-key, and version
authority drift.

Regression Verification

✓ The current checkout installed successfully as editable MaterialGraph
version `0.1.0`.

✓ Five focused configuration tests passed.

✓ The full regression suite passed with 589 tests.

✓ Ruff passed for all changed Python implementation and test files.

✓ A conflict search excluding frozen audit history and generated bytecode
returned no active conflicting keys, versions, repository URLs, or self-install
markers.

API Contract

Additive. `/health` now exposes the canonical installed package version.
FastAPI OpenAPI metadata uses the same value.

Database Migration

No.

Scientific Impact

No scientific data, scoring, ranking, graph, pathway, or evidence semantic
changed.

Operational Note

Deployments must install the current checkout with
`python -m pip install --no-deps -e .` after updating dependencies so runtime
package metadata follows the deployed commit.

Related Findings

- MG-AUD-055
- MG-AUD-093

# MG-AUD-059

Title

Normalized composition membership persistence

Status

✅ Resolved

Resolution Version

Post-v1.9.18

Affected Components

- Material import persistence
- Composition normalization regression coverage
- Material import regression coverage

Root Cause

The importer correctly resolved a stripped, deduplicated mapping of element
symbols to composition fractions, but `_link_elements` ignored that normalized
membership when choosing which rows to persist. It iterated the original raw
candidate element list and only used the normalized mapping for fraction
lookup. Duplicate raw membership could therefore attempt duplicate
`MaterialElement` rows for the same material and normalized element.

Resolution

✓ Changed material-element persistence to iterate the normalized composition
fraction mapping directly.

✓ Persisted each normalized symbol exactly once with its already-resolved
fraction.

✓ Preserved the existing legacy behavior that assigns `1.0` when composition
weights are unavailable.

✓ Added regression tests for whitespace-normalized duplicates, legacy duplicate
membership, normalized fraction resolution, and uniqueness of persisted links.

Regression Verification

✓ Twenty-five focused composition and import tests passed.

✓ The full regression suite passed with 592 tests.

✓ Ruff passed for the import service and both affected test modules.

API Contract

No change.

Database Migration

No. The existing `uq_material_element` uniqueness constraint remains valid and
acts as a final integrity guard.

Scientific Impact

Normalized composition membership is now preserved consistently from fraction
resolution through database persistence. No scoring, ranking, graph, pathway,
or evidence formulas changed.

Related Findings

- MG-AUD-001
- MG-AUD-004

# MG-AUD-016-017

Title

Explicit soft discovery policies and enforced strict avoidance

Status

✅ Resolved

Resolution Version

Post-v1.9.18

Affected Components

- Discovery candidate response contract
- Research-objective exploration response contract
- Candidate discovery service
- Objective exploration strict-mode filtering
- Discovery and objective-exploration API regression coverage

Root Cause

The candidate discovery pipeline implemented avoid and prefer inputs only as
score adjustments, but the response contract did not state that they were soft
constraints. Objective exploration accepted a `strict` mode yet only changed
penalties and warning text; it did not remove violating candidates or chains.

Resolution

✓ Added typed candidate-discovery policy metadata declaring avoided elements as
`soft_penalty` and preferred elements as `soft_bonus`.

✓ Added typed objective-exploration policy metadata distinguishing
`soft_penalty` from `hard_rejection` and declaring the hard-rejection scope.

✓ Enforced strict avoidance before ranking and limiting by rejecting any chain
whose non-root materials contain a requested avoided element.

✓ Excluded the root material from hard rejection so an objective can request
removal of an element present in its starting material.

✓ Preserved preferred elements as soft bonuses and preserved balanced and
exploratory scoring behavior.

✓ Removed warning text that described strict rejection as merely aspirational.

Regression Verification

✓ Forty-seven focused service and API tests passed.

✓ The full regression suite passed with 596 tests.

✓ Ruff passed for all affected schemas, services, and tests.

API Contract

Additive. Candidate discovery and objective exploration responses now include
typed `constraint_policy` metadata. Strict exploration can return fewer chains
and candidates because avoided-element violations are now rejected.

Database Migration

No.

Scientific Impact

Constraint enforcement now matches the declared exploration mode. Existing
candidate scoring constants and non-strict numerical behavior are unchanged.

Related Findings

- MG-AUD-004
- MG-AUD-009
- MG-AUD-015

# MG-AUD-018-020

Title

Composition-heuristic family and substitution semantics

Status

✅ Resolved

Resolution Version

Post-v1.9.18

Resolution Date

2026-08-19

Summary

Material-family and substitution relationships remain deterministic internal
composition heuristics, but their public metadata and explanations no longer
imply validated structural families or substitution mechanisms. The alkali
taxonomy is centralized and excludes Mg.

Implementation

✓ Added a canonical alkali-metal definition containing Li, Na, K, Rb, Cs, and
Fr, and reused it across family and discovery services.

✓ Excluded Mg from alkali-substitution classification.

✓ Added `relationship_basis="composition_heuristic"` and
`structural_family_validated=false` to family results.

✓ Added composition-heuristic basis and
`substitution_mechanism_validated=false` metadata to substitution paths,
transitions, and graph edges.

✓ Reworded explanations as composition candidates or hypotheses while
preserving relationship identifiers and numeric heuristic scores.

Regression Verification

✓ Forty-four focused domain, service, and API tests passed.

✓ The full regression suite passed with 600 tests.

✓ Ruff passed for all affected domain, service, schema, route, and test files.

API Contract

Additive. Family and discovery responses include explicit basis and validation
metadata. Explanatory wording is more conservative; existing relationship
identifiers and numerical scores are unchanged.

Database Migration

No.

Scientific Impact

Internal composition continuity is no longer presented as evidence of a
validated structural family, site equivalence, or substitution mechanism.

Related Findings

- MG-AUD-018
- MG-AUD-019
- MG-AUD-020

# MG-AUD-030-032

Title

Deterministic bounded discovery and search transparency

Status

✅ Resolved

Resolution Version

Post-v1.9.18

Resolution Date

2026-08-19

Summary

Discovery-chain response limits are separated from internal search work.
Objective evaluation now operates on the complete bounded pool, family
ordering no longer rewards correlated relationship-label multiplicity, and
equal candidates have a stable final tie-breaker.

Implementation

✓ Replaced result-limit-driven traversal termination with an independent
200-state bounded breadth-first search budget.

✓ Retained the existing six-candidate per-material expansion bound.

✓ Applied objective filtering and scientific ranking before the requested
result-limit slice.

✓ Replaced relationship-label-count priority with shared-element-count
ordering.

✓ Added ascending material ID as the deterministic final family-ordering
tie-breaker.

✓ Added typed search-policy, budget, count, truncation, and scientific-
completeness metadata to discovery-chain and objective responses.

Regression Verification

✓ Forty-one focused service and API tests passed.

✓ The full regression suite passed with 605 tests.

✓ Ruff passed for every affected service, schema, and test file.

API Contract

Additive. Discovery-chain and research-objective responses expose search
metadata. Existing result-limit query and request fields retain their public
meaning, but no longer terminate internal traversal.

Database Migration

No.

Scientific Impact

Search remains intentionally bounded for production safety. The response now
states that bounded discovery does not guarantee a scientifically complete
opportunity set.

Related Findings

- MG-AUD-030
- MG-AUD-031
- MG-AUD-032

# MG-AUD-040

Title

Causal transition-prefix attribution in objective exploration

Status

✅ Resolved

Resolution Version

Post-v1.9.18

Resolution Date

2026-08-20

Summary

Objective-exploration candidates are now scored and explained using only the
transitions that causally lead to each candidate, rather than every transition
in the completed chain.

Implementation

✓ Enumerated non-root materials by their position in each discovery chain.

✓ Passed `transitions[:material_index]` to candidate scoring and explanation.

✓ Preserved full causal history for endpoint candidates.

✓ Added a two-hop regression proving an intermediate candidate cannot inherit
an alkali-substitution bonus or reason from its outgoing future transition.

Regression Verification

✓ Twelve focused objective-exploration tests passed.

✓ The full regression suite passed with 606 tests.

✓ Ruff passed for the affected service and tests.

API Contract

No change.

Database Migration

No.

Scientific Impact

Candidate-level evidence and bonuses now reflect only the pathway prefix that
actually produced the candidate. Scoring constants and completed-path ranking
remain unchanged.

Related Finding

- MG-AUD-040

# MG-AUD-046-048

Title

Scoped and bulk material-composition loading

Status

✅ Resolved

Resolution Version

Post-v1.9.18

Resolution Date

2026-08-20

Affected Components

- Material family discovery
- Candidate screening
- Discovery candidate aggregation
- Material-family, screening, and candidate-service regression tests

Root Cause

Three related request paths loaded composition data at the wrong granularity.
Family discovery loaded the global material-element map and scanned all
materials in Python. Candidate screening performed a separate element lookup
for each material. Discovery candidate aggregation loaded another global map
after family discovery had already loaded the same class of data.

Resolution

✓ Family discovery now obtains candidate IDs through material-element overlap
with the base material and loads compositions only for that scoped set.

✓ The family service exposes an internal result-plus-composition-map operation
while preserving the existing public family-result method.

✓ Discovery candidate aggregation reuses the family service's scoped map.

✓ Recommendation and scenario candidates extend the request-local map only
for material IDs that are not already present.

✓ Candidate screening bulk-loads element symbols once and continues to call
the bulk risk-signal operation once.

✓ Existing relationship classification, screening policy, discovery scoring,
explanations, and deterministic ordering are preserved.

Regression Verification

✓ Twenty-six focused tests passed.

✓ Regressions verify scoped family composition loading, one bulk composition
and risk call in screening, and composition-map reuse in candidate discovery.

✓ The full regression suite passed with 609 tests.

✓ Ruff passed for all affected production and test files.

API Contract

No public contract change.

Database Migration

No.

Scientific Impact

No relationship rule, score, ranking policy, evidence meaning, or explanation
changed. The remediation reduces database work and removes query amplification
without altering scientific results.

Related Findings

- MG-AUD-046
- MG-AUD-047
- MG-AUD-048

# MG-AUD-033-035

Title

Complete candidate evaluation and explicit criticality policy

Status

✅ Resolved

Resolution Version

Post-v1.9.18

Resolution Date

2026-08-20

Affected Components

- Material similarity service and response schema
- Material recommendation and scenario services and response schemas
- Discovery candidate aggregation and scoring
- Material recommendation API routes
- Similarity, recommendation, discovery-scoring, candidate, and scenario API tests

Root Cause

Similarity sliced the neighbor list before calculating the complete downstream
ranking evidence. Recommendation and scenario stages therefore operated on an
irreversibly reduced pool. Lower-criticality preference was also enabled or
forced in several general-purpose paths, and discovery scoring applied an
additional fixed lower-criticality bonus after recommendation scoring.

Resolution

✓ Similarity evaluates and ranks all structured neighbors before applying its
result limit.

✓ Recommendation requests the complete similarity pool, calculates all
recommendation scores, and only then applies its final limit.

✓ Scenario evaluation receives the complete recommendation pool before final
scenario ranking and slicing.

✓ General recommendation, scenario, and discovery-candidate paths are neutral
by default; lower-criticality preference is an explicit API opt-in.

✓ Similarity retains known-and-closest criticality only as a tie-breaker after
equal similarity scores.

✓ Removed discovery's second fixed lower-criticality bonus.

✓ Added typed ranking-policy metadata, candidate-pool counts, returned counts,
and material-ID final tie-breaking.

Regression Verification

✓ Fifty-five focused service and API tests passed.

✓ Regressions cover complete pre-limit evaluation, neutral defaults, explicit
opt-in, policy disclosure, and absence of duplicate discovery weighting.

✓ The full regression suite passed with 614 tests.

✓ Ruff passed for every affected production and test file.

API Contract

Additive policy and count metadata. The general recommendation and scenario
criticality preference defaults changed from enabled to neutral; callers can
restore the former behavior explicitly with
`prefer_lower_criticality=true`.

Database Migration

No.

Scientific Impact

Candidates are no longer excluded before all relevant ranking stages can
evaluate them. Criticality preference is now deliberate and inspectable rather
than an implicit general default, and the same preference is not counted twice
in discovery scoring.

Related Findings

- MG-AUD-033
- MG-AUD-034
- MG-AUD-035

# MG-AUD-038-039

Title

Productive path efficiency and endpoint-sensitive material quality

Status

✅ Resolved

Resolution Version

Post-v1.9.18

Resolution Date

2026-08-20

Affected Components

- Discovery path ranking service
- Discovery path-ranking regression tests
- K-best path integration regression tests

Root Cause

Path efficiency used only broad hop-count bands. Equal-hop paths therefore
received identical efficiency credit even when one path undid an earlier
element event. Material quality used the arithmetic mean of every material on
the path, allowing common starting materials and intermediates to dilute
endpoint-quality differences.

Resolution

✓ Hop count remains the base path-efficiency cost.

✓ Efficiency is scaled by the proportion of transitions that do not reverse
an element introduction or removal already observed on the path.

✓ Transition plausibility remains independently scored; it is not reused as
an efficiency input.

✓ Whole-path arithmetic quality averaging was replaced by the geometric
composition of endpoint quality and the weakest path-material quality.

✓ Existing `path_efficiency` and `material_quality` response keys and their
10-point and 15-point allocations are preserved.

Regression Verification

✓ Twenty-eight focused path-ranking and K-best tests passed.

✓ Regressions distinguish productive and reversing equal-hop paths, preserve
endpoint-quality differentiation, retain weak-intermediate bottlenecks, and
reconcile the total score with its breakdown.

✓ The full regression suite passed with 618 tests.

✓ Ruff passed for the affected production and test files.

API Contract

No public contract change.

Database Migration

No.

Scientific Impact

Path rankings now distinguish productive progress from reversal without
conflating efficiency with transition plausibility. Material-quality ranking
retains endpoint sensitivity while continuing to penalize a weak material
anywhere on the path. These remain deterministic policy scores and do not
constitute independent experimental validation.

Related Findings

- MG-AUD-038
- MG-AUD-039

# MG-AUD-044

Title

Explicit candidate-comparison availability dispositions

Status

✅ Resolved

Resolution Version

Post-v1.9.18

Resolution Date

2026-08-20

Affected Components

- Candidate screening schema and service
- Candidate comparison schema and service
- Candidate comparison API route
- Candidate comparison service and API regression tests

Root Cause

Comparison received only candidates that survived bulk screening. Absence
from that result could mean the material did not exist, failed the stability
requirement, exceeded the energy-above-hull threshold, or was otherwise
unavailable, but all cases collapsed into the same null service result and
generic `404` response.

Resolution

✓ Added targeted screening evaluation for requested material IDs.

✓ Added explicit `eligible`, `material_not_found`, `filtered_unstable`,
`filtered_energy_above_hull`, and `unavailable` dispositions.

✓ Targeted evaluation reuses the same filter policy as bulk screening and
loads composition and risk evidence only for eligible requested materials.

✓ Comparison returns a typed unavailable result when either requested
candidate cannot be compared.

✓ The API maps nonexistent materials to structured `404` details and
constraint-filtered materials to structured `422` details.

✓ Successful comparison response fields and winner/tie semantics are
unchanged.

Regression Verification

✓ Seventeen focused screening, comparison-service, and comparison-API tests
passed.

✓ Regressions cover successful comparison, missing-material disposition,
filtered-material disposition, explicit ties, evidence-aware ordering, and
HTTP error mapping.

✓ The full regression suite passed with 620 tests.

✓ Ruff passed for every affected production and test file.

API Contract

Successful `200` responses are unchanged. Unsuccessful comparisons now expose
structured detail: missing materials use `404`, while existing materials
excluded by selected constraints use `422`.

Database Migration

No.

Scientific Impact

The comparison endpoint no longer misrepresents a constraint-based exclusion
as nonexistence. It exposes the deterministic reason comparison could not be
performed without changing candidate scores, winner selection, or tie
semantics.

Related Finding

- MG-AUD-044

## MG-AUD-045

Status

✅ Resolved

Resolution Version

Post-v1.9.18

Resolution Date

2026-08-21

Affected Components

- Scientific-pathway analysis response schemas
- Scientific-pathway confidence construction
- Research contract regression tests

Root Cause

The scientific-pathway response evolved after its initial schema was created.
Although producer behavior had stabilized, several fixed nested payloads still
used generic collections or dictionaries, and finite semantic states remained
unconstrained strings. This made the OpenAPI contract broader than the actual
implementation and left deterministic confidence scope implicit.

Resolution

✓ Added concrete models for pathway materials and transitions,
material-quality evidence, and the canonical scientific score breakdown.

✓ Added typed comparative models for pathway references, strengths,
trade-offs, research gaps, evidence readiness, assumptions, pairwise
comparisons, and element highlights.

✓ Added typed endpoint-sensitive ranking records, evidence groups, priority
groups, and status-specific result groups.

✓ Constrained evidence readiness, confidence level, objective satisfaction,
quality labels, comparison status, and endpoint differentiation status to the
values emitted by their producers.

✓ Added the explicit confidence scope
`deterministic_pathway_ranking_not_external_validation`.

✓ Replaced the generic pathway-comparison and endpoint-ranking alternatives in
the public analysis response with their concrete models.

✓ Preserved mappings where keys are intentionally extensible, including
discovery-candidate scoring and endpoint grouping-policy metadata.

Regression Verification

✓ Seven dedicated schema-contract tests passed.

✓ Seventy-eight focused research evidence, scientific pathway, comparative
intelligence, endpoint-ranking, and API tests passed.

✓ The full regression suite passed with 627 tests.

✓ Ruff passed for every affected production and test file.

API Contract

The scientific-pathway OpenAPI response now exposes concrete nested models and
finite semantic vocabularies. Existing valid response content is preserved;
unsupported states and malformed fixed-shape nested payloads no longer satisfy
the declared contract.

Database Migration

No.

Scientific Impact

No scientific score, ranking policy, tie behavior, or pathway-generation logic
changed. The remediation makes existing deterministic semantics inspectable
and prevents pathway confidence from being interpreted as external scientific
validation.

Related Finding

- MG-AUD-045

## MG-AUD-027

Status

✅ Resolved

Resolution Version

Post-v1.9.18

Resolution Date

2026-08-21

Affected Components

- Shared stability-evidence policy
- Material quality scoring and evidence metadata
- Material similarity scoring and ranking-policy metadata
- Material recommendation scoring and explanations
- Similarity, recommendation, and scientific-pathway schemas
- Stability-policy, service, contract, and API regression tests

Root Cause

The imported `is_stable` flag and quantitative `energy_above_hull` value were
treated as independent scoring evidence even though they describe correlated
thermodynamic stability information. Recommendation then added further
stability bonuses on top of a similarity score that already contained both
signals.

Resolution

✓ Added a centralized energy-primary stability-evidence policy.

✓ Energy above hull determines the stability band when present; the imported
stable flag is used only as incomplete fallback evidence when energy is absent.

✓ The policy reports evidence basis, completeness, and whether the two imported
sources are consistent.

✓ Material quality and similarity now apply one stability contribution each.

✓ Recommendation no longer reapplies stable or low-energy bonuses already
included in the similarity score.

✓ Added typed stability policy and provenance metadata to similarity,
recommendation, and nested material-quality response contracts.

✓ Preserved stable and energy-above-hull screening parameters as independent
hard request constraints rather than cumulative score contributions.

Regression Verification

✓ Fifty-eight focused policy, quality, similarity, recommendation, contract,
and scenario-API tests passed.

✓ The full regression suite passed with 639 tests.

✓ Ruff passed for every affected production and test file.

API Contract

Similarity and recommendation candidates now include additive stability band,
evidence basis, evidence completeness, source consistency, and score-
contribution fields. Ranking-policy metadata states that similarity uses a
single energy-primary signal and recommendation inherits that signal without
reapplying it.

Database Migration

No.

Scientific Impact

Correlated stability fields no longer receive repeated rewards in the same
decision pipeline. Scores and ordering may change where duplicate stability
weighting previously influenced results. The change does not claim that
thermodynamic stability proves synthesis feasibility or experimental validity.

Related Finding

- MG-AUD-027

## MG-AUD-054

Status

✅ Resolved

Resolution Version

Post-v1.9.18

Resolution Date

2026-08-21

Affected Components

- Shared discovery explanation construction
- Discovery candidate explanation regression coverage

Root Cause

The explanation formatter joined independently generated clauses with a
single space and assumed every producer supplied terminal punctuation. When
an evidence-limitation clause and a following score clause both lacked a
separator, the resulting text made them appear to be one statement.

Resolution

✓ Added centralized clause normalization to the shared discovery explanation
service.

✓ Trimmed surrounding whitespace from each explanation clause.

✓ Added terminal punctuation only when a clause does not already end with an
accepted terminator.

✓ Preserved existing clause content, ordering, and producer-supplied
punctuation.

Regression Verification

✓ Added a focused regression using the previously affected
`not validated` and `similarity score` clause boundary.

✓ Verified that the explanation contains
`not validated. similarity score 110.0.` and does not contain
`not validated similarity score`.

✓ The 29 focused candidate-service and discovery-candidate API tests passed.

✓ The full regression suite passed with 640 tests.

✓ Ruff passed for the explanation service and candidate-service test file.

API Contract

No schema or field change. Existing discovery explanations can gain terminal
punctuation where a producer omitted it.

Database Migration

No.

Scientific Impact

No numeric score, ranking, scientific classification, candidate selection,
or evidence policy changed. The remediation prevents separate explanation
clauses from appearing to form one unsupported scientific statement.

Related Finding

- MG-AUD-054
