# Remediation Verification Evidence

Create one evidence record per finding or tightly coupled batch. Record exact
commands, environment, database target class (never credentials), exit status,
test counts, relevant outputs, and unresolved limitations.

Historical test claims from the completed audit are contextual evidence only;
they cannot substitute for verification against the remediation checkout.
